export class Customers {
  constructor(public id: number, public id_key: number, public name: string, public customer_code: string, public mobile: string, public number_of_device: number, public device_type: number, public number_of_steps: number, public price: number, public maintenence_months: number, public address: Text, public line: string, public notes: Text, public date: Date, public day: number, public month: number) { }
}
